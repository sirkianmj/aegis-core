name: aegis-core
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  # --- CONDA PACKAGES (Stable System Libraries) ---
  - z3-solver=4.12.2
  - networkx=3.2.1
  - numpy=1.26
  
  # --- PIP PACKAGES (Specialized Tools) ---
  - pip:
    # The Analyst (Moved here to fix installation)
    - angr==9.2.78          # Pinning to a stable PyPI version
    - unicorn==2.0.1.post1
    - capstone==5.0.1
    
    # The Architect
    - pydantic==2.5.2
    - textual==0.44.1
    
    # The Weaponsmith
    - pwntools==4.11.0
    - pyvis==0.3.2
    
    # Quality & Security
    - pytest==7.4.3
    - mypy==1.7.1
    - bandit==1.7.5
    - black==23.11.0
    - flake8==6.1.0